{
  // 1. Root JSON as an OBJECT instead of an ARRAY
  // Many flows assume an array and break when a single object is returned
  "rootObjectExample": {
    "id": 101,
    "name": "John",
    "status": "Active"
  },

  // 2. Missing or optional fields across records
  // 'department' is missing in the first record
  "missingFieldsExample": [
    { "id": 1, "name": "John" },
    { "id": 2, "name": "Jane", "department": "HR" }
  ],

  // 3. Nested objects (need flattening for CSV/Excel)
  // CSV/Excel cannot handle nested structures directly
  "nestedObjectExample": {
    "employee": {
      "name": "John",     //employee_name
      "role": "Engineer" //employee_role
    },
    "status": "Active"
  },

  // 4. Arrays inside JSON
  // Arrays inside records must be joined or safely stringified
  "arrayInsideJsonExample": {
    "id": 10,
    "tags": ["urgent", "finance", "approval"] "urgent,finane"
  },

  // 5. Mixed data types (null, boolean, number, date)
  // Real data is rarely clean or consistent
  "mixedDataTypesExample": {
    "id": 5,
    "approved": true,
    "amount": 1250.75,
    "submittedOn": "2024-01-15",
    "remarks": null
  },

  // 6. Special characters that break CSV/Excel
  // Commas, quotes, and new lines must be escaped properly
  "specialCharactersExample": {
    "name": "John, Doe",
    "comment": "He said \"Approved\" on line 1\nThen updated it."
  },

  // 7. Schema changes without breaking existing exports
  // New fields appear later and should not break old flows
  "schemaChangeExample": [
    { "id": 1, "status": "Open" },
    { "id": 2, "status": "Closed", "priority": "High" }
  ]
}
